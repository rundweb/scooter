import React from 'react'

const Button = ({title}) => {
  return (
    <div>
      <a href="" className='bg-blue-primary text-white py-4 px-8 shadow-lg font-medium tracking-wide'>{title}</a>
    </div>
  )
}

export default Button
